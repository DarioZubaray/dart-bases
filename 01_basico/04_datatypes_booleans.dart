main() {
  var isActive;
  bool canJump = true;

  print(isActive); //null
  // el ! cambia el valor booleano a su opuesto, si esta al principio
  print(!canJump);
  print(canJump!); // indicar que el valor no es nulo, si esta al final
}
