main() {
  final double z;
  //print(z); //da error
  z = 0.1;
  // z = 0.1; // no se puede reasignar
  print(z);

  late final double x;
  //print(x); //da error
  x = 0.2;
  // x = 0.2; // no se puede reasignar
  print(x);
}
